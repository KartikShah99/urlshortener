apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: urlshortener-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_GITHUB_USERNAME/urlshortener
    targetRevision: main
    path: helm/urlshortener
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: urlshortener-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
