apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: urlshortener-{{ .Values.env }}
spec:
  selector:
    app: frontend
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: {{ .Values.frontend.port }}
